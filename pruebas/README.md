# Pruebas del proyecto

`pruebas.sql` contiene los casos preparados para validar reglas de integridad y requerimientos de los módulos seleccionados.

Las pruebas deben ejecutarse sobre una restauración local de PostgreSQL. Los resultados no se consideran verificados hasta que se ejecuten realmente.
